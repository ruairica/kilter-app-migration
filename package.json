{
  "name": "kilter-app-migration",
  "module": "src/main.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/main.ts",
    "build": "bun build src/main.ts --compile --outfile dist/kilter-migrate-windows && bun build src/main.ts --compile --target=bun-linux-x64 --outfile dist/kilter-migrate-linux && bun build src/main.ts --compile --target=bun-darwin-arm64 --outfile dist/kilter-migrate-macos-arm && bun build src/main.ts --compile --target=bun-darwin-x64 --outfile dist/kilter-migrate-macos-x64",
    "check": "bunx --bun @biomejs/biome check src/",
    "format": "bunx --bun @biomejs/biome check --write src/"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.8",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.3.2"
  }
}